<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <handlers>
            <add name="Python FastCGI" 
                 path="*" 
                 verb="*" 
                 modules="FastCgiModule" 
                 scriptProcessor="C:\doce-controle\venv\Scripts\python.exe|C:\doce-controle\venv\Lib\site-packages\wfastcgi.py" 
                 resourceType="Unspecified" 
                 requireAccess="Script" />
        </handlers>
    </system.webServer>
    <appSettings>
        <!-- Caminho para a raiz do seu projeto, para que o Python encontre o app.py -->
        <add key="PYTHONPATH" value="C:\doce-controle" />
        <!-- Informa ao servidor qual é o seu aplicativo Flask (arquivo app, objeto app) -->
        <add key="WSGI_HANDLER" value="app.app" />
        <!-- Define o arquivo principal do Flask -->
        <add key="FLASK_APP" value="app.py" />
    </appSettings>
</configuration>
```
